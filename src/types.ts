export interface Contact {
  id: number;
  name: string;
  title: string;
  company: string;
  email: string;
  phone: string;
  web: string;
  palIdx: number;
}

export interface Palette {
  bg: string;
  text: string;
  div: string;
}
